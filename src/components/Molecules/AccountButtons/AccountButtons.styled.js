import styled from 'styled-components';

const StyledAccountButtons = styled.nav`
  button {
    margin-left: 27px;
    font-size: 2.5rem;
    font-weight: bold;
  }
`;

export default StyledAccountButtons;
